This folder contains deprecated or superseded evaluation utilities.

- `eval_wp5_old_semantics.py`: disabled; official evaluation sets both-empty=1.0.
- `verify_present_only_eval.py`: disabled; use `scripts/eval_wp5.py` instead.

They are intentionally blocked from running to avoid accidental use.

